# fantasy_antfarm_v0.4
```
1) be sure all files (11) are in same folder
2) requires python 3.5+
3) for options, run: python fantfarm_v71.py --help
4) recommended settings: python fantfarm_v71.py -v --year 100 --pop-scale .2 --psum 1
    this will run sim for 100 years at 20% of the population_by_region cap (will speed up runtime), show all events, and print a summary report every 1 year
```


